# Frontend Design Index

本文档是新版前端设计入口。修改前端业务页前，先读对应 `design/frontend/*.md`；如果改动了页面职责、API payload、路由或关键组件，同步更新对应 design。

## 页面索引

- `AI Provider Settings`
  - route: `/settings/ai-provider`
  - design: `design/frontend/ai-provider.md`
  - code: `frontend/src/views/settings/ai-provider/index.tsx`, `frontend/src/lib/api.ts`

## 兼容说明

历史前端设计文档仍在 `design/front`。AI Provider 之后的新文档优先放在 `design/frontend`。
